apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msu-control.fullname" . }}-test-connection"
  labels:
    {{- include "msu-control.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msu-control.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
